# Server Configuration
server.port=${PORT:8080}

# Database Configuration (Supports Aiven, Railway, Render, or local MySQL)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/Telivision?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Hello@123}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration (minimum 256-bit strong secret key)
app.jwt.secret=${APP_JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
app.jwt.expiration-ms=86400000

# Verification Configuration
app.verification.otp-expiry-minutes=15

# Spring Mail SMTP Configuration (Gmail App Password Configuration)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SPRING_MAIL_USERNAME:anandswaroop5056@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:kmfn kkcn daja kdjl}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Razorpay Configuration
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_TK5SDHaYEtJcq3}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:YOUR_RAZORPAY_SECRET_KEY}
